# Terminus

Terminus is a self-hosted server for managing TRMNL e-paper devices and their content.

## Setup

Open Terminus after installation to complete its initial setup. Configure your TRMNL device to use the Terminus URL provided by Runtipi as its API endpoint.

The app URL must remain reachable by the devices that use it. Runtipi manages HTTPS and routing for the web service.